{
  "name": "infocooperazione-cli",
  "version": "1.0.0",
  "description": "CLI for the jobs category of Info Cooperazione (www.info-cooperazione.it) — Italian NGO and international-cooperation vacancies. Parses the site's public server-rendered pages; no account, zero runtime dependencies.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "infocooperazione-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "1.3.14"
  }
}
